## Worktree

You are working in an isolated git worktree with its own dependencies and dedicated dev servers on non-standard ports (offset: ${OFFSET}). This directory is the git root — not `../monster_master`. Branch: `${BRANCH}`(based on`origin/main`).
